[pytest]
minversion = 7.0
python_files = test_*.py
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: marks tests that hit real external systems or take longer to run
    selenium: marks UI automation tests that require a webdriver
    integration: marks tests that cover multiple application layers
